
// This is replaced during compilation by webpack define plugin
const VERSION = process.env.VERSION
// This is replaced during compilation by webpack patch plugin
const ASSETS = process.env.ASSETS

const CACHE_NAME = `sarigama-${ VERSION }`