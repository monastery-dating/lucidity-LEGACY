/*
import { ActionContextType } from '../../context.type'

export const valuesAction =
( { state
  , input: { values, pos }
  } : ActionContextType
) => {
  state.set ( [ '$playback', 'ctrl', pos ], values )
}
*/