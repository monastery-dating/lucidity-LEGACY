import { ActionContextType } from '../../context.type'
export const addAction =
( { state
  , input: { }
  , output
  } : ActionContextType
) => {
  // clear modal
  // add project

  // output ( { docs } )
}
