export default (options = {}) => {
  return (module, controller) => {
    /*
    module.addState
    ( { type: 'info'
      , message: ''
      }
    )

    module.addSignals
    ( {
      }
    )
    */

    return {} // meta information
  }
}
