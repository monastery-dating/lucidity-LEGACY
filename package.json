{
  "name": "@lucidity/monorepo",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "repository": "git@github.com:luciditeam/lucidity.git",
  "scripts": {
    "test": "CI=true lerna run test",
    "start": "lerna --scope lucy-editor run start",
    "deploy": "lerna --scope lucy-editor run deploy",
    "predeploy": "lerna bootstrap",
    "pretest": "lerna bootstrap"
  },
  "dependencies": {
    "dotenv": "^4.0.0",
    "gh-pages": "^0.12.0",
    "jest": "^19.0.2",
    "lerna": "2.0.0-beta.34",
    "ts-jest": "^19.0.8",
    "ts-loader": "^2.0.3",
    "typescript": "^2.2.2"
  }
}
